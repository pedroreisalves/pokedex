const INITIAL_STATE = {
  pokemons: [],
  length: 9,
}

const fetchReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default fetchReducer;